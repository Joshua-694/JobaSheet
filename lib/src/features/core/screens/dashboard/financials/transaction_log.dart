import 'package:flutter/material.dart';

class TransactionLogs extends StatelessWidget {
  const TransactionLogs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Transaction Logs"),
      ),
      backgroundColor: Colors.grey,
    );
  }
}
