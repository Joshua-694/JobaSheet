import 'package:flutter/material.dart';

class CurrentProjects extends StatelessWidget {
  const CurrentProjects({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Current Projects"),
      ),
      backgroundColor: Colors.grey,
    );
  }
}
