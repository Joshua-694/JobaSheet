import 'package:flutter/material.dart';

class FutureProjects extends StatelessWidget {
  const FutureProjects({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Future Projectss"),
      ),
      backgroundColor: Colors.grey,
    );
  }
}
