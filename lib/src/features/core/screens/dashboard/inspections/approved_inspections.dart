import 'package:flutter/material.dart';

class ApprovedInspections extends StatelessWidget {
  const ApprovedInspections({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Approved Inspections"),
      ),
      backgroundColor: Colors.grey,
    );
  }
}
